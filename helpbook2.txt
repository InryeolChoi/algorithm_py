<백트래킹 기법>
def backtrack(x):
    if 정답이라면?:
        정답 출력/저장
        return

    else 정답이 아니라면?:
        for 모든 뻗을 수 있는 자식노드에 관해:
            if 정답이 유망하다면:
                자식노드로 이동
                backtrack(x+1)
                부모노드로 이동

<dfs>
def dfs():
    asdf
dfs()

<bfs>
def bfs():
    asdf
bfs()